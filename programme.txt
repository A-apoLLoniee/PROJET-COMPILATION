#PYTHON

// Ceci est un commentaire sur une ligne
/* Ceci est un commentaire
   sur plusieurs lignes */

var x, y, z : entier;
var tableau[10] : reel;
var matrice[5][5] : entier;

debut
  x <- 10;
  y <- 20;
  z <- x + y * 2;
  tableau[0] <- 3.14;

  si x < y alors
    afficher(x);
  sinon
    afficher(y);
  finsi

  pour i <- 0; jusqua 10; faire
    tableau[i] <- i * 2;
  finpour

  repeter
    lire(x);
  tantque x <> 0;
fin
